import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const CoursesScreen = () => {
    return <Text style={{ fontSize: 48 }}>CoursesScreen</Text>;
};

const styles = StyleSheet.create({});

export default CoursesScreen;