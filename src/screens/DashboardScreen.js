import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const DashboardScreen = () => {
    return <Text style={{ fontSize: 48 }}>DashboardScreen</Text>;
};

const styles = StyleSheet.create({});

export default DashboardScreen;