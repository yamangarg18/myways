import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const SettingsScreen = () => {
    return <Text style={{ fontSize: 48 }}>SettingsScreen</Text>;
};

const styles = StyleSheet.create({});

export default SettingsScreen;